name: Delete Cluster

on:
  workflow_dispatch:
    inputs:
      cluster_id:
        description: 'Cluster ID'
        required: true
        type: string
      cluster_name:
        description: 'Cluster name to delete'
        required: true
        type: string
      subscription_id:
        description: 'Azure Subscription ID'
        required: true
        type: string

env:
  CLUSTER_ID: ${{ github.event.inputs.cluster_id }}
  CLUSTER_NAME: ${{ github.event.inputs.cluster_name }}
  SUBSCRIPTION_ID: ${{ github.event.inputs.subscription_id }}

jobs:
  delete:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests

      - name: Verify configuration
        run: |
          echo "🔍 Workflow Configuration:"
          echo "  Cluster ID: ${{ github.event.inputs.cluster_id }}"
          echo "  Cluster name: ${{ github.event.inputs.cluster_name }}"
          echo "  Subscription ID: ${{ github.event.inputs.subscription_id }}"
          echo ""
          echo "🔐 Authentication Method: Databricks Token (Built-in)"
          echo "ℹ Using Databricks API for cluster deletion"

      - name: Delete cluster
        env:
          CLUSTER_ID: ${{ github.event.inputs.cluster_id }}
          CLUSTER_NAME: ${{ github.event.inputs.cluster_name }}
          SUBSCRIPTION_ID: ${{ github.event.inputs.subscription_id }}
        run: |
          echo "🗑 Starting cluster deletion process..."
          echo "  Cluster ID: ${CLUSTER_ID}"
          echo "  Cluster Name: ${CLUSTER_NAME}"
          echo "  Subscription ID: ${SUBSCRIPTION_ID}"
          python delete_cluster.py "${CLUSTER_NAME}" "${CLUSTER_ID}" "" "${SUBSCRIPTION_ID}"
